import React from 'react';

const NavBar = () => (
  <div className="myNav">
    <h3>TARGET MVD</h3>
    <p> Create a  target  wherever on the map, specify your interest: Travel,
        Dating, Music, etc and start conecting with others who share your interest.</p>
  </div>
);

export default NavBar;
